<rax-scrollview 
    className="{{className}}" 
    style="{{style}}" 
    endReachedThreshold="{{endReachedThreshold}}"
    onEndReached="onEndReached"
    onScroll="onScroll">
    <slot/>
</rax-scrollview>
